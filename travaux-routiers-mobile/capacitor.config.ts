import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'travaux-routiers-mobile',
  webDir: 'dist'
};

export default config;
