# Firebase (Web)
VITE_FIREBASE_API_KEY=
VITE_FIREBASE_AUTH_DOMAIN=
VITE_FIREBASE_PROJECT_ID=
VITE_FIREBASE_STORAGE_BUCKET=
VITE_FIREBASE_MESSAGING_SENDER_ID=
VITE_FIREBASE_APP_ID=

# Spring Boot API
# If you run the backend via docker-compose, `app-java` is mapped to 8081 on host.
VITE_API_BASE_URL=http://localhost:8081

# Auth mode:
# - firebase: frontend signs in/up with Firebase, backend receives `{ idToken }` on `/api/auth/login`.
# - local: frontend calls backend with `{ usernameOrEmail, password }` on `/api/auth/login`.
VITE_AUTH_MODE=firebase

# Optional: if your backend prefix changes
# VITE_API_PREFIX=/api

# Admin API key (required for manager create-user endpoint)
# Must match backend env var: ADMIN_API_KEY
VITE_ADMIN_API_KEY=
